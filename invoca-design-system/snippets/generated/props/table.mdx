{/* GENERATED — do not hand-edit. Produced by tools/emit-components.mjs from Titan source. */}

This page covers 5 exports. What each one declares, and what it inherits:

| Export | Implementation | Props it declares | What the system decides |
|---|---|---|---|
| `DataGridPro` | Wrapper, adds nothing | **None** — inherited in full | Surface, row rule, header and cell type, selected row, cell focus ring, sort icon |
| `DataGridPremium` | Re-exported directly | — re-exported, no local source | Nothing beyond what the theme already applies |
| `GridToolbar` | Re-exported directly | — re-exported, no local source | Type and spacing of its contents |
| `GridActionsCellItem` | Re-exported directly | — re-exported, no local source | Nothing |
| `GridColumnMenu` | Re-exported directly | — re-exported, no local source | Nothing |

<Warning>
  **The accepted surface is wider than the decided one.** Where an export declares no props of
  its own, it accepts everything its base accepts — including options the system never decided
  and never gave a value. That something renders is not evidence the system offers it.

  Use the props the pages document. Do not reach for inherited API this documentation does
  not list.
</Warning>
