{/* GENERATED — do not hand-edit. Produced by tools/emit-components.mjs from Titan source. */}

This page covers 4 exports. What each one declares, and what it inherits:

| Export | Implementation | Props it declares | What the system decides |
|---|---|---|---|
| `Tabs` | Wrapper, adds nothing | **None** — inherited in full | The boundary rule, and the vertical rail's dimensions and selected treatment |
| `Tab` | Re-exported directly | — re-exported, no local source | Label casing, and the vertical row's type, colours, and selected marker |
| `TabIconLabel` | Titan implementation | `icon` `children` `endAdornment` | Icon gap, alignment, and label truncation |
| `TabMenu` | Titan implementation | `label` `menuItems` `onMenuChange` `value` `disabled` `id` | The menu trigger and its accessible name |

<Warning>
  **The accepted surface is wider than the decided one.** Where an export declares no props of
  its own, it accepts everything its base accepts — including options the system never decided
  and never gave a value. That something renders is not evidence the system offers it.

  Use the props the pages document. Do not reach for inherited API this documentation does
  not list.
</Warning>
