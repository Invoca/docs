{/* Sourced from the theme snapshot captured 2026-08-11 (`.titan-snapshot/`), not from a live
     run of `tools/emit-tokens.mjs` — no Titan checkout is available this pass. Re-verify
     against source once a checkout exists. See `.titan-snapshot/README.md`. */}

<Note>
  **No DatePicker-specific tokens were found in the theme snapshot.** The snapshot's probe set
  was built around foundational and simple-control tokens; nothing under a `date-*` or
  `calendar-*` prefix was captured. This is a coverage gap in the snapshot, not necessarily an
  absence in Titan source — unlike [tokens/alert.mdx](../tokens/alert.mdx), this is not marked
  "pending generation" only because the snapshot was checked and came back empty, not because
  the checkout is unavailable. Re-check directly against source once a checkout exists.
</Note>
