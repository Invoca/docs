{/* GENERATED PLACEHOLDER — replaced by the CI emitter. Do not hand-edit. */}

<Note>
  **Pending generation.** This table is emitted from a CI-refreshed `utilization.md` per
  component, which does not exist without a Titan checkout — `analyseUtilization()` in
  `tools/emit-components.mjs` returns nothing when that file is absent, and there is no
  fallback data source. See `snippets/generated/README.md`.
</Note>
